allow-unknown-data-type = true,
allow-missing-score = false,
save-evidence-scores = false,
evidence-file-name = "evidence_ese",
source-weights {
    include required(classpath("config/parts/pipeline-scoring-weights.conf"))
}