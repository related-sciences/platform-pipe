include required(classpath("config/parts/main.conf"))
pipeline {
    scoring {
        include required(classpath("config/parts/pipeline-scoring.conf"))
    }
    decorators {
        include required(classpath("config/parts/pipeline-decorators-unknown.conf"))
    }
}